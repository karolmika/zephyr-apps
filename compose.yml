services:
  zephyr:
    user: "${HOST_UID}:${HOST_GID}"
    build: .
    image: zephyr-dev-setup:1.0
    working_dir: /workdir
    volumes:
      - $HOME/work/zephyrproject:/workdir
    command: sleep infinity
