services:
  zephyr:
    user: "${HOST_UID}:${HOST_GID}"
    build: .
    image: zephyr-dev-setup:1.0
    working_dir: /workdir
    volumes:
      - ./:/workdir 
      - $HOME/.ssh:/root/.ssh:ro # Mount SSH keys for git access
    command: sleep infinity
