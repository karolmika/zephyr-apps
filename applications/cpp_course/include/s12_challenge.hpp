/**
 * @brief Multiplies two arrays element-wise
 *
 * Write a function that takes two arrays of integers and their size as input, 
 * and returns a new array that contains the product of the corresponding elements of the input arrays. 
 * The function should handle cases where the input arrays are of different sizes by returning an empty 
 * array or an error message.
 */
void multiply_arrays(void);